<%- include ('vistas') %>

    <% if (msj) { %>
        <div class="alert alert-danger" role="alert">
            <%= msj %>
        </div>
    <%  } %>

    <ul class="list_adopcion">
        <% if (data){%>
            <% for(var i=0; i<data.length; i++) { %>
                <li class="adopcion_lis">
                <div class="card" style="width: 18rem;">
                <div class="card-header">
                  Perro adoptado
                </div>
              <div class="card-body">
              <h5 class="card-title"><%= data[i].titulo %></h5>
              <h6 class="card-subtitle mb-2 text-muted">Nombre: <%= data[i].nombre %></h6>
              <p class="card-text">Descripcion: <%=data[i].texto%></p>
              </div>
              <% } %>
              <%} %>

    </ul>

<%- include ('partials/_footer') %>